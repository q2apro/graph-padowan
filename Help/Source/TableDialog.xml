<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE chapter SYSTEM "dtd/dblite.dtd" [
<!ENTITY % Symbols SYSTEM "Symbols.ent">
%Symbols;
]>
<section id="TableDialog">
	<?dbhh topicname="IDH_DIALOG_TABLE" topicid="150"?>
	<title>Table</title>
	<indexterm><primary>calc</primary></indexterm>
	<indexterm><primary>table</primary></indexterm>
	<indexterm><primary>evaluate</primary></indexterm>
	<para>
		The dialog shown below is used to evaluate the selected function for a range.
		First select a function in the <glossterm>function list</glossterm> and use the menu item
		<menuchoice><guimenu>Calc</guimenu><guimenuitem>Table</guimenuitem></menuchoice> to show the dialog.
		You specify the first and last value of the independent variable in the <guilabel>From</guilabel> and <guilabel>To</guilabel> fields.
		In the <guilabel><markup>&Delta;x</markup></guilabel> or <guilabel><markup>&Delta;t</markup></guilabel> field you specify the increment of the independent variable at each evaluation.
	</para>

	<para>
		When you press the <guibutton>Calc</guibutton> button,
		the table will be filled with the independent variable in the first column.
		The rest of the columns depend on the type of function.
		For a standard function, the table will show f(x), f'(x) and f''(x).
		For a parametric function, the table will show x(t), y(t), dx/dt, dy/dt and dy/dx.
		For a polar function, the table will show r(t), x(t), y(t), dr/dt and dy/dx.
		Unneeded columns can be hidden from the context menu.
		If the calculations takes a long time, a progress indicator will be shown.
	</para>

  <graphic fileref="DialogTable.png" align="center" scale="60"/>

	<para>
		You can select some cells with the mouse and <action>right click</action> with the mouse and use
		<guimenuitem>Copy</guimenuitem> from the context menu to copy the cells to the clipboard.
		From the clipboard the data may be pasted into another program, e.g. Microsoft Excel.
	</para>

	<para>
		When you move the mouse to the left side of the table the mouse pointer changes to a right pointing arrow.
		Now you can select whole rows with the mouse.
		When you move the mouse to the top of the table, the pointer changes to a down pointing arrow.
		Now you can select whole columns with the mouse.
		The whole table may be selected by <action>right clicking</action> and selecting <guimenuitem>Select all</guimenuitem>.
		It is also possible to select cells by holding down the <keycap>Shift</keycap> key and using the arrow keys on the keyboard.
	</para>

	<para>
		From the context menu it is also possible to export the selected data to a file as comma or tab delimited text.
	</para>

	<para>
		Note that if you choose to create a lot of values in the table, it may take some time to calculate them.
		Many values may also take up a lot of memory from the system.
	</para>
</section>
